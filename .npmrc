# NPM Registry Configuration for Iran
# این فایل به طور خودکار از میرورهای داخلی و بین‌المللی استفاده می‌کند

# Primary Registry: NPM Mirror Iran (پیشنهادی برای کاربران ایرانی)
registry=https://registry.npmmirror.com/

# Alternative registries (در صورت مشکل، به ترتیب امتحان شوند):
# registry=https://registry.npm.taobao.org/
# registry=https://registry.npmjs.org/

# Timeout settings for better performance in Iran
fetch-timeout=60000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# Strict SSL (در صورت مشکل با SSL می‌توانید false کنید)
strict-ssl=true

# Cache configuration
cache-min=86400

# Progress settings
progress=true
loglevel=info
